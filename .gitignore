node_modules/
dist/
*.log
.DS_Store

# Playwright runtime / Proofkeeper-owned content (traces, reports)
test-results/
playwright-report/
blob-report/
.proofkeeper/
traces/

# Compiler output (emitted specs are regenerated from recorded sessions)
examples/generated/

# Demo recording scratch (only the committed GIFs under docs/assets/ are kept)
demo/out/
